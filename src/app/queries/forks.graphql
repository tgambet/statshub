query Forks($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    forks(first: 100) {
      pageInfo {
        endCursor
        hasNextPage
      }
      edges {
        node {
          createdAt
        }
      }
    }
  }
}


